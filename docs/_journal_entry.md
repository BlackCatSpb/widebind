

## EVAL 8155=9.2149, 8388=9.1686; DepthController 12->16->20; ФИКС resume-глубины (2026-08-27)

- EVAL 8155: val_loss=9.2149, ppl=10045.82 -> new best. EVAL 8388: val_loss=9.1686, ppl=9591.61
  -> new best (9.2149->9.1686). val продолжает падать (9.36 -> 9.21 -> 9.17).
- DepthController естественно расширяется на каждом plateau: 7922=12/24, 8155=16/24, 8388=20/24.
  Следующий plateau-EVAL (~8621) -> 24/24. Глубокие слои сами доактивируются - контроллер работает.
- mem вырос 12.8 -> 13.5GB (по мере расширения активных слоёв - ожидаемо). mod_mlp=0.661 (спит).
  intent_w ~1.294 (стабилен на усиленном уровне). CE ~8.4-9.1 (step 8305=8.4183).
- ФИКС (commit): DepthController больше не сбрасывает глубину на 8 при resume. train.py и
  notebooks/colab.ipynb теперь СОХРАНЯЮТ 'active_depth' в чекпоинт и ВОССТАНАВЛИВАЮТ его при
  resume (фоллбэк на старую эвристику min(8+step//15000*4, n) для до-фикс чекпоинтов). Это
  реализует то, что уже было задумано в комментарии ячейки 10 ("continue from the checkpoint
  depth so all-24 never wake at once"). После деплоя фикса ВСЕ резюмы точно восстанавливают
  достигнутую глубину. Текущий резюм с до-фикс чекпоинта даст фоллбэк=8 и естественный реклимб
  (8->12->16->20->24 за ~4 EVAL, самоисправляется).
