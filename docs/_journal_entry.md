

## analyze best.pt(8155): MLP спит, w_intent плоский, pred мёртв, bus активен (2026-08-27)

- depth=16 на момент save. MLP метаядро СПИТ: sigmoid(mod_scale_mlp)=0.661 (как на 7922),
  MLP W_std dev +0.0007 vs decay-кривой (по сути заморожено). Самый большой нетронутый рычаг.
- Bridge w_intent ПЛОСКИЙ на всех слоях: L0=0.1419, L12=0.0347, L23=0.0164 (без изменений с 6757).
  L12 активен при depth=16, но его w_intent не двигается; L23 заморожен (depth16<23).
  => intent-шина работает как фиксированный кондуит, ёмкость не углубляется с глубиной.
- bus_head_proj вес обучен (weight_norm=0.6519, рост с 0). bus_bias крупный (~434-808, осциллирует).
  SIGNALS одинаковы по слоям (temp/pred/smooth/sym/help равны) => intent НЕ дифференцирован по слоям.
- pred head МЁРТВ (requires_grad=False, без градиента). diversity слабо выровнен с CE
  (cos_sim=0, град почти не добавляется) - тот же [WATCH].
- Рождение экспертов в средних слоях [10,11,12,13,15] (как на 7922) - эксперты всё ещё "кипят"
  в середине, не в глубине.
- Вывод: val падает (9.17@8388) за счёт постепенной разморозки глубоких слоёв + экспертов;
  MLP и углубление bridge-весов - нетронутый резерв. Фикс resume-глубины (b7e2a43) даст
  deep-слоям доучиться без реклимба.
