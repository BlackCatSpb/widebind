

## best.pt(8388) запатчен active_depth=20; старые step_*.pt вынесены -> резюм depth=20 (2026-08-27)

- Пользователь выполнил сниппет: старые step_*.pt перенесены в _old_steps/, best.pt(8388) записан
  active_depth=20. Резюм -> auto-resume не находит step_*.pt и берёт best.pt(8388) на depth=20
  (реклимб пропущен, продолжаем с достигнутой глубины).
- Ожидание: следующий EVAL (~8621) -> DepthController plateau-expand 20->24/24 (финальное
  расширение). val стартует с 9.1686, должен чуть улучшиться за счёт активации слоёв 20-23.
- Далее все 24 слоя активны; модель идёт чистой ёмкостью. MLP метаядро остаётся спящим
  (sigmoid(mod_scale_mlp)=0.661) - нетронутый резерв для будущих экспериментов.
- Фикс b7e2a43 теперь полностью в силе: все save этого сеанса пишут active_depth, будущие
  резюмы точны.
