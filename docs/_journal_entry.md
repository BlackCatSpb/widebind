

## Резюм @8388: depth сброшен в 8 (фоллбэк), val 9.17->12.0951, mem 13.5->12.5GB (2026-08-27)

- Резюм с Drive best.pt(8388): "Training: step 8388 -> 300000". EVAL 8388 СРАЗУ = val_loss=12.0951
  (ppl 178999) - РЕЗКО ХУЖЕ довозобновного 9.1686. mem 13.5->12.5GB (меньше активных слоёв),
  tok/s прогрев 29->63, lr сброшен на пик 3e-4 (LRController пересобран).
- Причина: Drive-чекпоинт сохранён ДО-ФИКС кодом => нет ключа 'active_depth' => фикс (b7e2a43)
  дал фоллбэк min(8 + 8388//15000*4, 24) = 8. DepthController 20->8: 16 слоёв заморожены =>
  модель, привыкшая к 20 слоям, деградировала; mem упал.
- mod_mlp=0.661 (спит), intent_w~1.29 (как до), pred мёртв, diversity слабо выровнен с CE.
- Контроллер реклимбнет 8->12->16->20->24 на plateau-EVAL (slope > -k*sigma). После резюма lr на
  пике => val сначала резко упадёт (strong LR), расширение начнётся только на plateau (~через
  ~1600 шагов до первого +4, итого ~step 11000 до 24/24). val восстановится.
- Фикс работает штатно: ВСЕ save ЭТОГО сеанса запишут active_depth, поэтому СЛЕДУЮЩИЙ резюм
  будет точным. Текущий реклимб - плата за старый чекпоинт (ожидаемо).
- ОПЦИЯ (пропустить реклимб и вернуть depth=20 сразу): остановить сеанс, на Colab вынести старые
  step_*.pt в подпапку (чтобы auto-resume не выбрал их), вшить active_depth=20 в best.pt(8388) и
  резюмнуть -> auto-resume возьмёт best.pt(8388) на depth=20, next eval (~8621) -> 24/24.
