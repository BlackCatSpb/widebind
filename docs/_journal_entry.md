

## EVAL 8621=9.1295 (new best), но depth НЕ расширился до 24 (остался 20) (2026-08-27)

- EVAL 8621: val_loss=9.1295 (ppl 9223.59) - new best (лучше довозобновных 9.1686 и старта 10.5287).
  Saved best to best.pt.
- DepthController НЕ расширил 20->24: нет сообщения "[DepthController] ... 24/24", mem остался
  12.5GB (при depth=24 было бы ~13.5GB). Причина: val РЕЗКО упал 10.53->9.13 (-1.4 за 233 шага,
  эффект un-choke после сброса LR на пик 3e-4) => slope сильно отрицательный => условие расширения
  (plateau: slope > -k*sigma) НЕ выполнено. Контроллер корректно ждёт плато, а не гонит глубину
  пока val улучшается.
- КОРРЕКЦИЯ моего прогноза: расширение до 24 произойдёт не на 8621, а когда val выйдет на plateau
  (как в прошлом прогоне: ~1631 шаг от резюма до первого +4). Ожидаемо ~step 9500-11000.
- Слои 16-23 всё ещё слабые/старые (L16-19 активированы только на 8388 ~0 шагов; L20-23 заморожены
  с 5825) => их действительно надо "добирать" обучением, а не только активацией. Чекпоинт 1.6GB =
  модель + optimizer state (Adam ~3x params, fp32) - норма для training-чекпоинта, не аномалия.
- val продолжает падать (9.1295) - модель ещё далека от насыщения при ~2.2M токенов; глубокие слои
  доучатся по ходу, и только после выхода на 24/24 генерация восстановит когерентность уровня 5825.
