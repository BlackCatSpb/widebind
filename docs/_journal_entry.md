

## analyze best.pt(8388, depth=20): bridge статичен, MLP спит, val улучшился только за счёт +слоёв (2026-08-27)

- depth=20 (layers 16-19 активировались на EVAL 8388). val_loss=9.1686 (ppl=9591.61) - new best (9.2149->9.1686).
- Bridge-веса ПОЛНОСТЬЮ СТАТИЧНЫ 8155->8388: w_intent L0=0.1418, L12=0.0346, L23=0.0164
  (разница в 3-м знаке). intent_probe W_norm 19.6042->19.5905. TAU LADDER неизменна
  (tau[0]=7.963 tau[-1]=531.609 alpha[0]=0.8415 alpha[-1]=0.9976). cross-layer cosine 0.0070 (дополняют, не дублируют).
- => Улучшение val (9.21->9.17) вызвано ИСКЛЮЧИТЕЛЬНО разморозкой слоёв (16->20), а НЕ
   углублением intent-шины. Шина - фиксированный кондуит фиксированной ёмкости; глубокие слои
   просто гонят тот же статичный сигнал.
- MLP СПИТ: sigmoid(mod_scale_mlp)=0.661 (как на 8155/7922), W_std dev +0.0007 vs decay-кривой.
  Главный нетронутый рычаг.
- BUS вырос: _last_bus norm 3002->3259, per-expert 518->570 (больше активных экспертов гонят шину).
  bus_head_proj weight_norm 0.6519->0.6515 (стабилен), bus_bias ~365 (осциллирует).
- pred head МЁРТВ (requires_grad=False). diversity слабо выровнен с CE (cos=0). Рождение экспертов
  в средних слоях [10,11,12,13,15] (неизменно).
- Вывод: модель "масштабируется числом слоёв", но не учится эффективнее. MLP-метаядро - единственный
  крупный резерв, ещё не тронутый. Следующий EVAL (~8621) -> depth=24/24.
