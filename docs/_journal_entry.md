

## Резюм B: depth снова сброшен в 8 (best.pt(8621) нет active_depth) -> 8854=12/24 (2026-08-27)

- Перечитал core/adaptation.py DepthController.update(): ТОЛЬКО УВЕЛИЧИВАЕТ active (line 141:
  min(active+inc, max_depth)), НЕ уменьшает. Сообщение "8854 -> active_depth=12/24" = первое
  расширение 8->12 => текущая сессия СТАРТОВАЛА с depth=8, а не 20.
- Причина: резюм взял best.pt(8621), в котором НЕТ active_depth (сохранён предыдущей сессией без
  save-фикса). Фикс-ноутбук прочитал ckpt.get('active_depth')=None -> фоллбэк min(8+8621//15000*4,24)=8.
  "патч не сработал" = персистентность не сработала, т.к. резюмируемый чекпоинт не нёс ключ.
- КОД ФИКСА КОРРЕКТЕН (проверил repo notebooks/colab.ipynb: resume-read + 3 save-write
  'active_depth' на месте). Он восстановит глубину ТОЛЬКО если чекпоинт резюма содержит active_depth.
- ХОРОШО: веса deep-слоёв, обученные при depth=20 в сессии A, сохранены в best.pt(8621); сбросился
  только depth-флаг (requires_grad). Реклимб 8->12->16->20->24 восстановит их быстро (веса не потеряны).
- ДЕЙСТВИЕ: чтобы вернуть depth=20 сейчас - запатчить резюмируемый чекпоинт active_depth=20 и
  резюмнуть (фикс-ноутбук далее запишет active_depth в свои saves -> точные резюмы). Либо дать
  сессии B доклимбиться до 24 самой (веса те же), после чего её saves будут нести active_depth.
- val продолжает падать: EVAL 8854=9.0861 (ppl 8831) - new best, несмотря на depth=8.
