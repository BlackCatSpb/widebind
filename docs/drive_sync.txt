=== Что обновить на Google Drive ===

Место на Drive: /content/drive/MyDrive/widebind_data/
(или где у вас лежат исходники)

Заменить вручную:
  1. core/config.py — Option C: mlp_groups=16, mlp_expand=4, mirror_k=16
  2. core/model.py — gate=|pred_error|, W_pred aux loss, pred cache, wd=0
  3. notebooks/colab.ipynb — Option C параметры, surv=2.0
  4. scripts/colab_train.py — без изменений (читает из config)
  5. scripts/mini_test.py — (опционально, для локальных тестов)

Можно скопировать командой:
  !cp /content/drive/MyDrive/widebind_data/src/core/config.py /content/widebind_src/core/config.py
  !cp /content/drive/MyDrive/widebind_data/src/core/model.py /content/widebind_src/core/model.py
  !cp /content/drive/MyDrive/widebind_data/src/notebooks/colab.ipynb /content/widebind_src/notebooks/colab.ipynb

Или целиком:
  !rm -rf /content/widebind_src
  !cp -r /content/drive/MyDrive/widebind_data/src /content/widebind_src

=== Последнее обновление (2026-07-14, Option C) ===
- **Option C+**: G=16, d=224, expand=4, mirror_k=32, surv=K/G=2.0
  16 экспертов по 224, каждый получает 2 K-space dim через bind.
  Зеркало видит 32 K-space чисел на эксперта (d/k=7:1).
  MLP 224→896→224: expand=4, скрытая размерность 896.
- **Params**: ~224M (было 221M, разница в большем mirror_k)
- **Совместимость**: checkpoint step_10000 несовместим (G=16≠32) — нужен fresh start
